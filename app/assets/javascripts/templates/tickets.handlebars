<div id="winningContainer">
  {{#if lastWinner}}
    {{view this.winningTicketView}}
  {{/if}}
</div>

<button {{action "pick"}}>Pick!</button>

{{#if winnerList}}
  <h2>Winner List</h2>
  <ul>
  {{#each winnerList}}
    <li>
    {{name}}
    </li>
  {{/each}}
  </ul>
{{/if}}

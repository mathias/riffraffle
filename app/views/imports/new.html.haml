%p Warning! Using this form will wipe out all old Tickets!
%p Only supports Tito Master Orders sheets right now!

= form_tag({action: 'create'}, multipart: true) do
  = label_tag :csv_file, "Select File"
  = file_field_tag :csv_file
  = submit_tag
